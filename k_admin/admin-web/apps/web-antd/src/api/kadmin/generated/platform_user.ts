// Code generated by kadmin codegen. DO NOT EDIT.
import { request } from '../client';

export interface PlatformUser {
  userId: number;
  userName: string;
  password: string;
  avatar: string;
  count: number;
  status: string;
  creatBy: string;
  createTime: string;
  updateBy: string;
  updateTime: string;
  remark: string;
}

export interface PlatformUserPayload {
  userName: string;
  password: string;
  avatar: string;
  count: number;
  status: string;
  creatBy: string;
  createTime: string;
  updateBy: string;
  updateTime: string;
  remark: string;
}

export interface PlatformUserFilters {
  page?: number;
  pageSize?: number;
  keyword?: string;
  userName?: string;
  password?: string;
  avatar?: string;
  status?: string;
  creatBy?: string;
  createTime?: string;
  updateBy?: string;
  updateTime?: string;
  remark?: string;
}

export interface PageResult<T> {
  items: T[];
  page: number;
  pageSize: number;
  total: number;
}

function queryString<T extends object>(filters: T) {
  const params = new URLSearchParams();
  Object.entries(filters).forEach(([key, value]) => {
    if (value !== undefined && value !== '') {
      params.set(key, String(value));
    }
  });
  const query = params.toString();
  return query ? `?${query}` : '';
}

export function getPlatformUserList(filters: PlatformUserFilters = {}) {
  return request<PageResult<PlatformUser>>(`/api/platform-users${queryString(filters)}`);
}

export function getPlatformUser(id: number) {
  return request<PlatformUser>(`/api/platform-users/${id}`);
}

export function createPlatformUser(payload: PlatformUserPayload) {
  return request<PlatformUser>(`/api/platform-users`, {
    method: 'POST',
    body: JSON.stringify(payload),
  });
}

export function updatePlatformUser(id: number, payload: PlatformUserPayload) {
  return request<PlatformUser>(`/api/platform-users/${id}`, {
    method: 'PUT',
    body: JSON.stringify(payload),
  });
}

export function deletePlatformUser(id: number) {
  return request<boolean>(`/api/platform-users/${id}`, { method: 'DELETE' });
}

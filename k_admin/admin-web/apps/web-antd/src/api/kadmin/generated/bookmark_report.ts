// Code generated by kadmin codegen. DO NOT EDIT.
import { request } from '../client';

export interface BookmarkReport {
  reportId: number;
  bookmarkId: number;
  userId: number;
  reason: string;
  result: string;
  createBy: string;
  createTime: string;
  updateBy: string;
  updateTime: string;
  remark: string;
}

export interface BookmarkReportPayload {
  bookmarkId: number;
  userId: number;
  reason: string;
  result: string;
  createBy: string;
  createTime: string;
  updateBy: string;
  updateTime: string;
  remark: string;
}

export interface BookmarkReportFilters {
  page?: number;
  pageSize?: number;
  keyword?: string;
  reason?: string;
  result?: string;
  createBy?: string;
  createTime?: string;
  updateBy?: string;
  updateTime?: string;
  remark?: string;
}

export interface PageResult<T> {
  items: T[];
  page: number;
  pageSize: number;
  total: number;
}

function queryString<T extends object>(filters: T) {
  const params = new URLSearchParams();
  Object.entries(filters).forEach(([key, value]) => {
    if (value !== undefined && value !== '') {
      params.set(key, String(value));
    }
  });
  const query = params.toString();
  return query ? `?${query}` : '';
}

export function getBookmarkReportList(filters: BookmarkReportFilters = {}) {
  return request<PageResult<BookmarkReport>>(`/api/bookmark-reports${queryString(filters)}`);
}

export function getBookmarkReport(id: number) {
  return request<BookmarkReport>(`/api/bookmark-reports/${id}`);
}

export function createBookmarkReport(payload: BookmarkReportPayload) {
  return request<BookmarkReport>(`/api/bookmark-reports`, {
    method: 'POST',
    body: JSON.stringify(payload),
  });
}

export function updateBookmarkReport(id: number, payload: BookmarkReportPayload) {
  return request<BookmarkReport>(`/api/bookmark-reports/${id}`, {
    method: 'PUT',
    body: JSON.stringify(payload),
  });
}

export function deleteBookmarkReport(id: number) {
  return request<boolean>(`/api/bookmark-reports/${id}`, { method: 'DELETE' });
}

// Code generated by kadmin codegen. DO NOT EDIT.
import { request } from '../client';

export interface Notification {
  notifyId: number;
  notifyType: string;
  title: string;
  content: string;
  status: string;
  sort: number;
  displayMode: string;
  faultStartTime: string;
  faultEndTime: string;
  maintenanceStartTime: string;
  maintenanceEndTime: string;
  remindBeforeMinutes: number;
  uploadUserId: number;
  materialId: number;
  materialTitleSnapshot: string;
  publishStart: string;
  publishEnd: string;
  scrollTimeInterval: number;
  createBy: string;
  createTime: string;
  updateBy: string;
  updateTime: string;
  remark: string;
}

export interface NotificationPayload {
  notifyType: string;
  title: string;
  content: string;
  status: string;
  sort: number;
  displayMode: string;
  faultStartTime: string;
  faultEndTime: string;
  maintenanceStartTime: string;
  maintenanceEndTime: string;
  remindBeforeMinutes: number;
  uploadUserId: number;
  materialId: number;
  materialTitleSnapshot: string;
  publishStart: string;
  publishEnd: string;
  scrollTimeInterval: number;
  createBy: string;
  createTime: string;
  updateBy: string;
  updateTime: string;
  remark: string;
}

export interface NotificationFilters {
  page?: number;
  pageSize?: number;
  keyword?: string;
  notifyType?: string;
  title?: string;
  content?: string;
  status?: string;
  displayMode?: string;
  faultStartTime?: string;
  faultEndTime?: string;
  maintenanceStartTime?: string;
  maintenanceEndTime?: string;
  materialTitleSnapshot?: string;
  publishStart?: string;
  publishEnd?: string;
  createBy?: string;
  createTime?: string;
  updateBy?: string;
  updateTime?: string;
  remark?: string;
}

export interface PageResult<T> {
  items: T[];
  page: number;
  pageSize: number;
  total: number;
}

function queryString<T extends object>(filters: T) {
  const params = new URLSearchParams();
  Object.entries(filters).forEach(([key, value]) => {
    if (value !== undefined && value !== '') {
      params.set(key, String(value));
    }
  });
  const query = params.toString();
  return query ? `?${query}` : '';
}

export function getNotificationList(filters: NotificationFilters = {}) {
  return request<PageResult<Notification>>(`/api/ptmj-notifications${queryString(filters)}`);
}

export function getNotification(id: number) {
  return request<Notification>(`/api/ptmj-notifications/${id}`);
}

export function createNotification(payload: NotificationPayload) {
  return request<Notification>(`/api/ptmj-notifications`, {
    method: 'POST',
    body: JSON.stringify(payload),
  });
}

export function updateNotification(id: number, payload: NotificationPayload) {
  return request<Notification>(`/api/ptmj-notifications/${id}`, {
    method: 'PUT',
    body: JSON.stringify(payload),
  });
}

export function deleteNotification(id: number) {
  return request<boolean>(`/api/ptmj-notifications/${id}`, { method: 'DELETE' });
}

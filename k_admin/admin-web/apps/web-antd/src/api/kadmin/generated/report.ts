// Code generated by kadmin codegen. DO NOT EDIT.
import { request } from '../client';

export interface Report {
  reportId: number;
  fileId: number;
  userId: number;
  reason: string;
  result: string;
  createBy: string;
  createTime: string;
  updateBy: string;
  updateTime: string;
  remark: string;
}

export interface ReportPayload {
  fileId: number;
  userId: number;
  reason: string;
  result: string;
  createBy: string;
  createTime: string;
  updateBy: string;
  updateTime: string;
  remark: string;
}

export interface ReportFilters {
  page?: number;
  pageSize?: number;
  keyword?: string;
  reason?: string;
  result?: string;
  createBy?: string;
  createTime?: string;
  updateBy?: string;
  updateTime?: string;
  remark?: string;
}

export interface PageResult<T> {
  items: T[];
  page: number;
  pageSize: number;
  total: number;
}

function queryString<T extends object>(filters: T) {
  const params = new URLSearchParams();
  Object.entries(filters).forEach(([key, value]) => {
    if (value !== undefined && value !== '') {
      params.set(key, String(value));
    }
  });
  const query = params.toString();
  return query ? `?${query}` : '';
}

export function getReportList(filters: ReportFilters = {}) {
  return request<PageResult<Report>>(`/api/reports${queryString(filters)}`);
}

export function getReport(id: number) {
  return request<Report>(`/api/reports/${id}`);
}

export function createReport(payload: ReportPayload) {
  return request<Report>(`/api/reports`, {
    method: 'POST',
    body: JSON.stringify(payload),
  });
}

export function updateReport(id: number, payload: ReportPayload) {
  return request<Report>(`/api/reports/${id}`, {
    method: 'PUT',
    body: JSON.stringify(payload),
  });
}

export function deleteReport(id: number) {
  return request<boolean>(`/api/reports/${id}`, { method: 'DELETE' });
}

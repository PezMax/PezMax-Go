// Code generated by kadmin codegen. DO NOT EDIT.
import { request } from '../client';

export interface UserSecurity {
  id: number;
  userId: number;
  question: string;
  answer: string;
  createBy: string;
  createTime: string;
  updateBy: string;
  updateTime: string;
  remark: string;
}

export interface UserSecurityPayload {
  userId: number;
  question: string;
  answer: string;
  createBy: string;
  createTime: string;
  updateBy: string;
  updateTime: string;
  remark: string;
}

export interface UserSecurityFilters {
  page?: number;
  pageSize?: number;
  keyword?: string;
  question?: string;
  answer?: string;
  createBy?: string;
  createTime?: string;
  updateBy?: string;
  updateTime?: string;
  remark?: string;
}

export interface PageResult<T> {
  items: T[];
  page: number;
  pageSize: number;
  total: number;
}

function queryString<T extends object>(filters: T) {
  const params = new URLSearchParams();
  Object.entries(filters).forEach(([key, value]) => {
    if (value !== undefined && value !== '') {
      params.set(key, String(value));
    }
  });
  const query = params.toString();
  return query ? `?${query}` : '';
}

export function getUserSecurityList(filters: UserSecurityFilters = {}) {
  return request<PageResult<UserSecurity>>(`/api/user-security${queryString(filters)}`);
}

export function getUserSecurity(id: number) {
  return request<UserSecurity>(`/api/user-security/${id}`);
}

export function createUserSecurity(payload: UserSecurityPayload) {
  return request<UserSecurity>(`/api/user-security`, {
    method: 'POST',
    body: JSON.stringify(payload),
  });
}

export function updateUserSecurity(id: number, payload: UserSecurityPayload) {
  return request<UserSecurity>(`/api/user-security/${id}`, {
    method: 'PUT',
    body: JSON.stringify(payload),
  });
}

export function deleteUserSecurity(id: number) {
  return request<boolean>(`/api/user-security/${id}`, { method: 'DELETE' });
}

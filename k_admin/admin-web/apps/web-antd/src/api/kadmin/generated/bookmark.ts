// Code generated by kadmin codegen. DO NOT EDIT.
import { request } from '../client';

export interface Bookmark {
  id: number;
  userId: number;
  url: string;
  title: string;
  description: string;
  coverImage: string;
  subject: string;
  resourceType: string;
  collection: string;
  status: number;
  delFlag: number;
  createBy: string;
  createTime: string;
  updateBy: string;
  updateTime: string;
  remark: string;
}

export interface BookmarkPayload {
  userId: number;
  url: string;
  title: string;
  description: string;
  coverImage: string;
  subject: string;
  resourceType: string;
  collection: string;
  status: number;
  delFlag: number;
  createBy: string;
  createTime: string;
  updateBy: string;
  updateTime: string;
  remark: string;
}

export interface BookmarkFilters {
  page?: number;
  pageSize?: number;
  keyword?: string;
  url?: string;
  title?: string;
  description?: string;
  coverImage?: string;
  subject?: string;
  resourceType?: string;
  collection?: string;
  createBy?: string;
  createTime?: string;
  updateBy?: string;
  updateTime?: string;
  remark?: string;
}

export interface PageResult<T> {
  items: T[];
  page: number;
  pageSize: number;
  total: number;
}

function queryString<T extends object>(filters: T) {
  const params = new URLSearchParams();
  Object.entries(filters).forEach(([key, value]) => {
    if (value !== undefined && value !== '') {
      params.set(key, String(value));
    }
  });
  const query = params.toString();
  return query ? `?${query}` : '';
}

export function getBookmarkList(filters: BookmarkFilters = {}) {
  return request<PageResult<Bookmark>>(`/api/bookmarks${queryString(filters)}`);
}

export function getBookmark(id: number) {
  return request<Bookmark>(`/api/bookmarks/${id}`);
}

export function createBookmark(payload: BookmarkPayload) {
  return request<Bookmark>(`/api/bookmarks`, {
    method: 'POST',
    body: JSON.stringify(payload),
  });
}

export function updateBookmark(id: number, payload: BookmarkPayload) {
  return request<Bookmark>(`/api/bookmarks/${id}`, {
    method: 'PUT',
    body: JSON.stringify(payload),
  });
}

export function deleteBookmark(id: number) {
  return request<boolean>(`/api/bookmarks/${id}`, { method: 'DELETE' });
}

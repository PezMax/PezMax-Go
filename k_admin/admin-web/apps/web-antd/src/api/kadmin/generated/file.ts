// Code generated by kadmin codegen. DO NOT EDIT.
import { request } from '../client';

export interface File {
  fileId: number;
  userId: number;
  fileName: string;
  fileUrl: string;
  fileSize: number;
  fileFormat: string;
  fileYear: number;
  fileType: number;
  fileSchool: string;
  fileSubject: string;
  reviewer: string;
  fileStatus: number;
  delFlag: number;
  createBy: string;
  createTime: string;
  updateBy: string;
  updateTime: string;
  remark: string;
}

export interface FilePayload {
  fileName: string;
  fileUrl: string;
  fileSize: number;
  fileFormat: string;
  fileYear: number;
  fileType: number;
  fileSchool: string;
  fileSubject: string;
  reviewer: string;
  fileStatus: number;
  delFlag: number;
  createBy: string;
  createTime: string;
  updateBy: string;
  updateTime: string;
  remark: string;
}

export interface FileFilters {
  page?: number;
  pageSize?: number;
  keyword?: string;
  fileName?: string;
  fileUrl?: string;
  fileFormat?: string;
  fileSchool?: string;
  fileSubject?: string;
  reviewer?: string;
  createBy?: string;
  createTime?: string;
  updateBy?: string;
  updateTime?: string;
  remark?: string;
}

export interface PageResult<T> {
  items: T[];
  page: number;
  pageSize: number;
  total: number;
}

function queryString<T extends object>(filters: T) {
  const params = new URLSearchParams();
  Object.entries(filters).forEach(([key, value]) => {
    if (value !== undefined && value !== '') {
      params.set(key, String(value));
    }
  });
  const query = params.toString();
  return query ? `?${query}` : '';
}

export function getFileList(filters: FileFilters = {}) {
  return request<PageResult<File>>(`/api/exam-files${queryString(filters)}`);
}

export function getFile(id: number) {
  return request<File>(`/api/exam-files/${id}`);
}

export function createFile(payload: FilePayload) {
  return request<File>(`/api/exam-files`, {
    method: 'POST',
    body: JSON.stringify(payload),
  });
}

export function updateFile(id: number, payload: FilePayload) {
  return request<File>(`/api/exam-files/${id}`, {
    method: 'PUT',
    body: JSON.stringify(payload),
  });
}

export function deleteFile(id: number) {
  return request<boolean>(`/api/exam-files/${id}`, { method: 'DELETE' });
}

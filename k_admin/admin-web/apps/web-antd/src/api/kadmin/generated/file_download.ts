// Code generated by kadmin codegen. DO NOT EDIT.
import { request } from '../client';

export interface FileDownload {
  downloadId: number;
  fileId: number;
  userId: number;
  creatBy: string;
  creatTime: string;
  updateBy: string;
  updateTime: string;
  remark: string;
}

export interface FileDownloadPayload {
  creatBy: string;
  creatTime: string;
  updateBy: string;
  updateTime: string;
  remark: string;
}

export interface FileDownloadFilters {
  page?: number;
  pageSize?: number;
  keyword?: string;
  creatBy?: string;
  creatTime?: string;
  updateBy?: string;
  updateTime?: string;
  remark?: string;
}

export interface PageResult<T> {
  items: T[];
  page: number;
  pageSize: number;
  total: number;
}

function queryString<T extends object>(filters: T) {
  const params = new URLSearchParams();
  Object.entries(filters).forEach(([key, value]) => {
    if (value !== undefined && value !== '') {
      params.set(key, String(value));
    }
  });
  const query = params.toString();
  return query ? `?${query}` : '';
}

export function getFileDownloadList(filters: FileDownloadFilters = {}) {
  return request<PageResult<FileDownload>>(`/api/file-downloads${queryString(filters)}`);
}

export function getFileDownload(id: number) {
  return request<FileDownload>(`/api/file-downloads/${id}`);
}

export function createFileDownload(payload: FileDownloadPayload) {
  return request<FileDownload>(`/api/file-downloads`, {
    method: 'POST',
    body: JSON.stringify(payload),
  });
}

export function updateFileDownload(id: number, payload: FileDownloadPayload) {
  return request<FileDownload>(`/api/file-downloads/${id}`, {
    method: 'PUT',
    body: JSON.stringify(payload),
  });
}

export function deleteFileDownload(id: number) {
  return request<boolean>(`/api/file-downloads/${id}`, { method: 'DELETE' });
}

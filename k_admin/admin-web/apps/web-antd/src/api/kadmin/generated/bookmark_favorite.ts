// Code generated by kadmin codegen. DO NOT EDIT.
import { request } from '../client';

export interface BookmarkFavorite {
  bookmarkId: number;
  userId: number;
}

export interface BookmarkFavoritePayload {
  userId: number;
}

export interface BookmarkFavoriteFilters {
  page?: number;
  pageSize?: number;
}

export interface PageResult<T> {
  items: T[];
  page: number;
  pageSize: number;
  total: number;
}

function queryString<T extends object>(filters: T) {
  const params = new URLSearchParams();
  Object.entries(filters).forEach(([key, value]) => {
    if (value !== undefined && value !== '') {
      params.set(key, String(value));
    }
  });
  const query = params.toString();
  return query ? `?${query}` : '';
}

export function getBookmarkFavoriteList(filters: BookmarkFavoriteFilters = {}) {
  return request<PageResult<BookmarkFavorite>>(`/api/bookmark-favorites${queryString(filters)}`);
}

export function getBookmarkFavorite(id: number) {
  return request<BookmarkFavorite>(`/api/bookmark-favorites/${id}`);
}

export function createBookmarkFavorite(payload: BookmarkFavoritePayload) {
  return request<BookmarkFavorite>(`/api/bookmark-favorites`, {
    method: 'POST',
    body: JSON.stringify(payload),
  });
}

export function updateBookmarkFavorite(id: number, payload: BookmarkFavoritePayload) {
  return request<BookmarkFavorite>(`/api/bookmark-favorites/${id}`, {
    method: 'PUT',
    body: JSON.stringify(payload),
  });
}

export function deleteBookmarkFavorite(id: number) {
  return request<boolean>(`/api/bookmark-favorites/${id}`, { method: 'DELETE' });
}

// Code generated by kadmin codegen. DO NOT EDIT.

package bookmark_favorite

import (
	"errors"
	"fmt"
	"strconv"
	"strings"
	"time"

	"github.com/GoAdminGroup/go-admin/modules/db"
)

var errBookmarkFavoriteNotFound = errors.New("bookmark_favorite not found")

type repository struct {
	conn db.Connection
}

func newRepository(conn db.Connection) *repository {
	return &repository{conn: conn}
}

func (r *repository) list(filter BookmarkFavoriteFilter) (BookmarkFavoritePage, error) {
	where, args := bookmark_favoriteWhere(filter)
	countRows, err := r.conn.Query(`SELECT count(*) AS count FROM public.ptmj_bookmark_favorite `+where, args...)
	if err != nil {
		return BookmarkFavoritePage{}, err
	}
	total := int64(0)
	if len(countRows) > 0 {
		total = toInt64(countRows[0]["count"])
	}
	queryArgs := append(append([]interface{}{}, args...), filter.PageSize, (filter.Page-1)*filter.PageSize)
	rows, err := r.conn.Query(`SELECT bookmark_id, user_id FROM public.ptmj_bookmark_favorite `+where+` ORDER BY bookmark_id DESC LIMIT ? OFFSET ?`, queryArgs...)
	if err != nil {
		return BookmarkFavoritePage{}, err
	}
	items := make([]BookmarkFavorite, 0, len(rows))
	for _, row := range rows {
		items = append(items, mapBookmarkFavorite(row))
	}
	return BookmarkFavoritePage{Items: items, Total: total, Page: filter.Page, PageSize: filter.PageSize}, nil
}

func bookmark_favoriteWhere(filter BookmarkFavoriteFilter) (string, []interface{}) {
	conditions := make([]string, 0, 4)
	args := make([]interface{}, 0, 4)
	if len(conditions) == 0 {
		return "", args
	}
	return "WHERE " + strings.Join(conditions, " AND "), args
}

func (r *repository) findByID(id int64) (BookmarkFavorite, bool, error) {
	rows, err := r.conn.Query(`SELECT bookmark_id, user_id FROM public.ptmj_bookmark_favorite WHERE bookmark_id = ?`, id)
	if err != nil {
		return BookmarkFavorite{}, false, err
	}
	if len(rows) == 0 {
		return BookmarkFavorite{}, false, nil
	}
	return mapBookmarkFavorite(rows[0]), true, nil
}

func (r *repository) mustFind(id int64) (BookmarkFavorite, error) {
	item, found, err := r.findByID(id)
	if err != nil {
		return BookmarkFavorite{}, err
	}
	if !found {
		return BookmarkFavorite{}, errBookmarkFavoriteNotFound
	}
	return item, nil
}

func (r *repository) create(payload BookmarkFavoritePayload) (BookmarkFavorite, error) {
	rows, err := r.conn.Query(`INSERT INTO public.ptmj_bookmark_favorite (user_id) VALUES (?) RETURNING bookmark_id`, payload.UserId)
	if err != nil {
		return BookmarkFavorite{}, fmt.Errorf("create bookmark_favorite: %w", err)
	}
	return r.mustFind(toInt64(rows[0]["bookmark_id"]))
}

func (r *repository) update(id int64, payload BookmarkFavoritePayload) (BookmarkFavorite, error) {
	result, err := r.conn.Exec(`UPDATE public.ptmj_bookmark_favorite SET user_id = ? WHERE bookmark_id = ?`, payload.UserId, id)
	if err != nil {
		return BookmarkFavorite{}, fmt.Errorf("update bookmark_favorite %d: %w", id, err)
	}
	affected, _ := result.RowsAffected()
	if affected == 0 {
		return BookmarkFavorite{}, errBookmarkFavoriteNotFound
	}
	return r.mustFind(id)
}

func (r *repository) delete(id int64) error {
	result, err := r.conn.Exec(`DELETE FROM public.ptmj_bookmark_favorite WHERE bookmark_id = ?`, id)
	if err != nil {
		return fmt.Errorf("delete bookmark_favorite %d: %w", id, err)
	}
	affected, _ := result.RowsAffected()
	if affected == 0 {
		return errBookmarkFavoriteNotFound
	}
	return nil
}

func mapBookmarkFavorite(row map[string]interface{}) BookmarkFavorite {
	return BookmarkFavorite{
		BookmarkId: toInt64(row["bookmark_id"]),
		UserId:     toInt64(row["user_id"]),
	}
}

func toInt64(value interface{}) int64 {
	switch typed := value.(type) {
	case int:
		return int64(typed)
	case int32:
		return int64(typed)
	case int64:
		return typed
	case float64:
		return int64(typed)
	case []byte:
		parsed, _ := strconv.ParseInt(string(typed), 10, 64)
		return parsed
	case string:
		parsed, _ := strconv.ParseInt(typed, 10, 64)
		return parsed
	default:
		return 0
	}
}

func toFloat64(value interface{}) float64 {
	switch typed := value.(type) {
	case float32:
		return float64(typed)
	case float64:
		return typed
	case int64:
		return float64(typed)
	case []byte:
		parsed, _ := strconv.ParseFloat(string(typed), 64)
		return parsed
	case string:
		parsed, _ := strconv.ParseFloat(typed, 64)
		return parsed
	default:
		return 0
	}
}

func toBool(value interface{}) bool {
	switch typed := value.(type) {
	case bool:
		return typed
	case string:
		parsed, _ := strconv.ParseBool(typed)
		return parsed
	case []byte:
		parsed, _ := strconv.ParseBool(string(typed))
		return parsed
	default:
		return false
	}
}

func toString(value interface{}) string {
	switch typed := value.(type) {
	case string:
		return typed
	case []byte:
		return string(typed)
	case nil:
		return ""
	default:
		return fmt.Sprint(typed)
	}
}

func timeText(value interface{}) string {
	if value == nil {
		return ""
	}
	if parsed, ok := value.(time.Time); ok {
		return parsed.In(time.Local).Format("2006-01-02 15:04:05")
	}
	text := strings.TrimSpace(toString(value))
	for _, layout := range []string{
		time.RFC3339Nano,
		"2006-01-02 15:04:05.999999999-07",
		"2006-01-02 15:04:05",
	} {
		if parsed, err := time.Parse(layout, text); err == nil {
			return parsed.In(time.Local).Format("2006-01-02 15:04:05")
		}
	}
	return text
}

// optionalTime maps an empty string to NULL for nullable date/time columns.
func optionalTime(value string) interface{} {
	if strings.TrimSpace(value) == "" {
		return nil
	}
	return value
}
